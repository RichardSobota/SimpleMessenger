You are an expert API designer. Generate a valid OpenAPI 3.0 YAML specification for a simple messenger API that allows creating, getting, updating, and deleting users and messages. The User resource should have the following fields: id as a server-generated UUID string, firstName and lastName as strings between 1 and 100 characters, email as a valid and unique email address, createdAt as a date-time string with timezone information and followers as list of users. The API should have the following endpoints: create a new user, get user details by id, add follower by id, remove follower by id, update user details by id, and delete a user by id. The create user endpoint accepts firstName, lastName, email, and password in the request body. Password should be between 8 and 24 characters. Never return the password in any API response. On success, return HTTP 201 with the created user in the response body and a Location header pointing to the new user's URL. Return a validation error if any field is invalid or missing, and a conflict error if the email already exists. The create user endpoint is public and does not require authentication. The create user endpoint accepts firstName, lastName, email, and password in the request body. The Message resource should have the following fields: id as a server-generated UUID string, user as UUID string which refers to User entity, messageText as string, and sentAt as a date-time string with timezone information. The API should have the following endpoints: create a new message, getMessageDetails by message id, getMessagesForUser by user id, delete message by id, and getMessages (which will return all messages, results should be pageable). All other endpoints should require JWT bearer token authentication by default. Use application/json as the only content type. Include examples for the request body and all response types. Return only the YAML with no extra commentary. If anything is unclear, please ask me a question.
